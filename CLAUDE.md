# Project notes for szviz

## Workflow preferences

- **Solo project.** Push directly to `main`. Do **not** create feature branches
  or pull requests unless explicitly asked — commit and push straight to `main`.
